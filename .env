# pandora_pm/.env

# --- Flask Configuration ---
FLASK_APP=run.py
FLASK_ENV=development # Use 'production' in production
SECRET_KEY='replace_with_a_very_strong_random_secret_key'

# --- MongoDB Connection Details ---
# Replace with MongoDB server IP address or hostname
MONGO_HOST=0.0.0.0 # Your provided IP

# Replace with MongoDB port (default is 27017)
MONGO_PORT=27017

# Replace with your MongoDB username (if authentication is enabled)
MONGO_USER=abc

# Replace with your MongoDB password (if authentication is enabled)
# Ensure special characters like '@' are URL-encoded if typed directly here.
# Reading from environment variables is generally safer.
MONGO_PASSWORD=abc@abc # Your provided URL-encoded password

# Specify the database where the user is defined (often 'admin' or the specific DB)
# Required if using authentication with a user defined outside the target DB.
MONGO_AUTH_DB=admin

# *** CRITICAL: Specify the database name for YOUR application data ***
DB_NAME=pandora_pm_db # Or any name you prefer for this app's data


# --- Initial Application Admin User Credentials (for flask init-admin command) ---
# Set these environment variables OR uncomment and set values here (less secure)
# PANDORA_ADMIN_USERNAME=admin
# PANDORA_ADMIN_EMAIL=admin@example.com
# PANDORA_ADMIN_PASSWORD=a_very_secure_password_change_me